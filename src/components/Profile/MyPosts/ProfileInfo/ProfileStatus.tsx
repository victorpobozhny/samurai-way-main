import s from './ProfileInfo.module.css'import React from "react";import {ProfileType} from "../../../../redux/profile-reducer";type ProfileStatusProps = {    profile: ProfileType}export class ProfileStatus extends React.Component<ProfileStatusProps> {    state = {        editMode: false,        title: 'yo'    }    activateEditMode() {        this.setState({            editMode: true        })    }    disableEditMode() {        this.setState({            editMode: false        })    }    render() {        return (            <div className={s.profileStatus}>                {                    !this.state.editMode &&                    <div                        onDoubleClick={this.activateEditMode.bind(this)}>Status: {this.props.profile.status}                    </div>                }                {                    this.state.editMode &&                    <div>                        <input                            type={"text"}                            onBlur={this.disableEditMode.bind(this)}                            autoFocus={true}                            value={this.state.title}                        />                    </div>                }            </div>        )    }}